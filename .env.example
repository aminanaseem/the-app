# Copy to .env (never commit .env). Export these in your shell or set them in
# your MCP client config when running the Linear MCP server or agents/linear.mjs.

# Required — Linear API key. Get one at https://linear.app/settings/api
LINEAR_API_KEY=

# Optional — Linear team key to scope operations. Defaults to THE (THE APP team).
LINEAR_TEAM_KEY=THE